(function($) {
  // Argument passed from InvokeCommand.
  $.fn.AjaxCallback = function(argument) {

    console.log(argument);
    // Set textfield's value to the passed arguments.
    //$('input#edit-deporte').attr('options', argument);
  };
})(jQuery);
